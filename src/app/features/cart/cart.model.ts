export interface ICartItem {
  //Para mi, debería ser un objeto de tipo ICart, pero vamos así
  id: string,
  title: string,
  url : string,
  imdbID : string,
  price : number,
  exists : boolean,

  // id: string,
  // movie: any,
}

